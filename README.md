# Netshoes Cart Test

* Clone this repo and then install dependecies with `npm install`
* this project uses gulp as task runner
* in your command line run `gulp`
* Project is running at http://localhost:8000